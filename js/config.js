// Configuration file to manage environment-specific settings
const config = {
  // API base URL - will be updated based on environment
  apiBaseUrl: 'https://portfolio-app-backend-yasg.onrender.com'
};

export default config; 